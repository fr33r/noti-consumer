/**
 * Defines the interface for an application module. An application module represents a logical
 * component that must be configured upon application startup.
 */
public interface ApplicationModule {

  void configure();
}
